# ============================================
# Environment Variables Example (Dummy Values)
# ============================================

# Server
PORT=8000

# JWT Secret (use a long random string in real env)
JWT_SECRET=your-jwt-secret-here

# MongoDB Connection URL
MONGO_URL=mongodb+srv://username:password@cluster.mongodb.net/database-name

# Admin Login Credentials (used to seed/authenticate the admin user)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-me

# AI Provider Keys (Gemini is primary; OpenAI is used as a fallback)
GEMINI_API_KEY=your-gemini-api-key
OPENAI_API_KEY=your-openai-api-key

# Instagram DM Integration
# Page access token from Meta Developer Console (with instagram_manage_messages permission)
INSTAGRAM_ACCESS_TOKEN=your-instagram-page-access-token
# Custom string YOU create — paste the same value in Meta Developer Console when registering the webhook
INSTAGRAM_VERIFY_TOKEN=acolorfultale_webhook_secret_2026
